# SEC EDGAR identity credentials
edgar-identity.txt

# Machine learning models
*.h5
*.keras
*.pkl
*.pickle
*.joblib
*.pt
*.pth
*.ckpt
*.pb
*.onnx
*.tflite
*.bin
*.safetensors
*.model
*.mar
*.weights

# Large datasets
*.csv
*.txt
*.json
*.parquet
*.feather
*.xlsx
*.xls
data/

# Track deterministic test fixtures even though *.json is ignored above.
!tests/fixtures/
!tests/fixtures/**

# Track committed operator artefacts even though the blanket *.json rule above
# ignores them. These are version-controlled deployment assets, not data.
!deploy/
!deploy/**/*.json

# The API image's compiled, hash-pinned dependency lock (F26), despite the
# blanket *.txt rule above. Load-bearing twice: git must track it, and
# `gcloud builds submit` (no .gcloudignore) derives its upload set from this
# file — without the carve-out Cloud Build never receives the lock.
!deploy/requirements.txt

# Track the vendored model-metadata/pricing catalogue even though *.json is
# ignored above. This is reviewed, secret-scanned package data, not runtime
# output.
!src/sec_generative_search/providers/data/
!src/sec_generative_search/providers/data/*.json

# ...but NEVER the operator-supplied runtime secrets / TLS material that live
# under deploy/. These re-ignores must stay AFTER the !deploy/ carve-out above.
# deploy/.env is already caught by the .env rule below; these are not.
deploy/secrets/
deploy/certs/

# Track the frontend project manifest + every .json under frontend/ EXCEPT
# the build artefacts (node_modules, .next, coverage, etc.) which are still
# blocked by their own folder ignore rules below. The blanket `*.json` rule
# above is for the Python project's data tree; the Node project needs its
# manifests committed for CI / Corepack / pnpm to bootstrap at all.
!frontend/
!frontend/**/*.json
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/coverage/

# Operating system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor temporary files
*.swp
*.swo
*~
*.tmp

# Backup files
*.bak
*.backup
*.old

# Git merge artifacts
*.orig
*.rej

# Environment variables and secrets
.env
.env.*
!.env.example
.envrc

# Certificates and private keys
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx

# API keys and tokens
*.token
secrets.json
client_secret.json
credentials.json
.netrc

# Cloud provider credentials
.aws/
.gcp/
.azure/

# Docker credentials
.dockercfg
.docker/config.json

# Database files
*.db
*.sqlite
*.sqlite3
db.sqlite3-journal
chroma_db/

# Log files
*.log

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff
local_settings.py

# Flask stuff
instance/
.webassets-cache

# Scrapy stuff
.scrapy

# Sphinx build output
docs/_build/

# Project build output
docs/
.claude/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# Scratch / investigation notebooks (not part of the project deliverable)
notebooks/*_investigation.ipynb

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
# Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
uv.lock
uv_*

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
# poetry.lock
# poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
# pdm.lock
# pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#   Pixi creates a virtual environment in the .pixi directory. It is recommended not to include
#   this directory in version control.
# pixi.lock
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Virtual environments
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm / JetBrains IDEs
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
.idea/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file.
.vscode/
*.code-workspace

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Ruff
.ruff_cache/

# PyPI configuration file
.pypirc

# Abstra
#  Abstra is an AI-powered process automation framework.
#  Ignore directories containing user credentials, local state, and settings.
#  Learn more at https://abstra.io/docs
.abstra/

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/
